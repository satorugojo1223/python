# Example tuple
numbers = (2, 3, 4, 5)

# Initialize product as 1
product = 1

# Multiply each number in the tuple
for num in numbers:
    product *= num

print("The product of the tuple is:", product)